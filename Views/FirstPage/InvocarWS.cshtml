
@model app.Models.Provider;
@{
    ViewBag.Title = "Evoke existing Web Service";
    <!-- var entidades = ViewBag.Entidades; -->
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "";
    <!-- var valueEntidade = " ";
    var test = ""; -->
}

<!DOCTYPE html>

<html>
<head>
<h2>@ViewBag.Title</h2>
</head>
<body>
  <div class="row">
    <form method="post">
      <label class="control-label">URL: </label>
      <input asp-for="url" placeholder="Web Service URL here"/>

      <label class="control-label">Entity: </label>
      <select asp-for="selected_table_name" asp-items="@new SelectList(Model.table_names)">
        <option>-- Select Entity --</option>
      </select> 
      <br/>

      <div>
        <form asp-action="getTablePost" asp-controller="FirstPage">
          <input type="submit" value="Mapping" name="map">
        </form>
      <div>

      <div>
        <label class="control-label">coluna teste</label>
        <select asp-for="column" asp-items="@new SelectList(Model.columns)">
          <option>-- Select column --</option>
        </select>
      </div>
    </form>
  </div> 

  </div>

  <div class="form-group">
    <form method="GET">
      <label class="control-label">coluna teste</label>
      <select asp-for="column" asp-items="@new SelectList(Model.columns)">
      </select>
      <p>@Model.columnNumber</p>
      <p>@ViewBag.selected_tabela</p>
      <p>@Model.url</p>
    </form>
  </div>

</body>


  <!-- <script src="~wwwroot/lib/jquery/dist/jquery.min.js"></script>
  <script type="text/javascript"> -->
  

  <!-- </script> -->
